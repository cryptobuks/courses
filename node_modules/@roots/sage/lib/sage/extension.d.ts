import { Extension } from '@roots/bud-framework/extension';
/**
 * roots/sage support extension
 *
 * @public
 * @decorator `@label`
 * @decorator `@dependsOn`
 */
export default class Sage extends Extension {
    /**
     * `boot` callback
     *
     * @public
     * @decorator `@bind`
     */
    register({ isProduction, path }: {
        isProduction: any;
        path: any;
    }): Promise<void>;
}
//# sourceMappingURL=extension.d.ts.map