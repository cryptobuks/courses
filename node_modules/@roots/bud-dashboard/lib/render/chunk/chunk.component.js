import { Box } from '@roots/bud-support/ink';
import React from '@roots/bud-support/react';
import { longestAssetNameLength } from '../format.js';
import Asset from './asset.component.js';
const Chunk = ({ final, indent, minWidth, ...chunk }) => {
    return (React.createElement(Box, { flexDirection: "column" }, chunk?.assets?.map((asset, index) => (React.createElement(Asset, { key: index, ...asset, emitted: chunk?.emitted, minWidth: longestAssetNameLength(chunk?.assets), final: index == chunk?.assets.length - 1, indent: [!final] })))));
};
export default Chunk;
//# sourceMappingURL=chunk.component.js.map