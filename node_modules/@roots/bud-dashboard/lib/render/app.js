import { Box, Text } from '@roots/bud-support/ink';
import React from '@roots/bud-support/react';
import Compilation from './compilation/compilation.component.js';
import { Server } from './server.js';
const App = ({ app, compilations, isTTY = true, displayServerInfo = true, displayAssets = true, displayEntrypoints = true, }) => {
    return (React.createElement(Box, { flexDirection: "column" },
        compilations.map((compilation, id) => (React.createElement(Box, { key: id, flexDirection: "column" },
            React.createElement(Box, null,
                React.createElement(Text, null, ` `)),
            React.createElement(Compilation, { id: id, mode: app.mode, stats: compilation, context: app.context, compilerCount: compilations.length, displayAssets: displayAssets, displayEntrypoints: displayEntrypoints }),
            React.createElement(Box, null,
                React.createElement(Text, null, ` `))))),
        app.isDevelopment ? (React.createElement(Server, { app: app, displayServerInfo: displayServerInfo })) : null));
};
export default App;
//# sourceMappingURL=app.js.map