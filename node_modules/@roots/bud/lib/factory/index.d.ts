import type { Options } from '@roots/bud-framework';
import Bud from '../bud.js';
/**
 * Cached instances
 *
 * @public
 */
declare let instances: Array<Bud>;
declare const get: (basedir?: string) => Promise<Bud>;
/**
 * Create a {@link Bud} instance programatically
 *
 * @example
 * ```ts
 * const bud = await factory()
 * ```
 *
 * @example
 * Running in a particular mode
 *
 * ```ts
 * const bud = await factory({mode: 'development'})
 * ```
 *
 * @returns Bud instance
 *
 * @public
 */
export declare function factory(config?: Options.Overrides, skipCache?: boolean): Promise<Bud>;
export { get, instances };
//# sourceMappingURL=index.d.ts.map