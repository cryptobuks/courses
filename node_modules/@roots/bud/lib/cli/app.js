/* eslint-disable no-console */
import { Builtins, Cli } from '@roots/bud-support/clipanion';
let instance;
export const get = (label, version) => {
    if (instance)
        return instance;
    instance = new Cli({
        binaryLabel: label,
        binaryName: label,
        binaryVersion: version,
        enableCapture: false,
        enableColors: true,
    });
    return instance;
};
export { Builtins, Cli };
//# sourceMappingURL=app.js.map