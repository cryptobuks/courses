import { Extension } from '@roots/bud-framework';
import { ManifestPluginOptions as Options, WebpackManifestPlugin } from 'webpack-manifest-plugin';
/**
 * `webpack-manifest-plugin` adapter
 *
 * @public
 * @decorator `@label`
 * @decorator `@plugin`
 * @decorator `@options`
 */
export default class BudWebpackManifestPlugin extends Extension<Options, WebpackManifestPlugin> {
    /**
     * `when` callback
     *
     * @remarks
     * Returns `feature.manifest` hook result
     *
     * @public
     * @decorator `@bind`
     */
    when(): Promise<boolean>;
}
//# sourceMappingURL=index.d.ts.map