import { Extension } from '@roots/bud-framework';
import Webpack from 'webpack';
export default class BudDefine extends Extension<Record<string, any>, Webpack.DefinePlugin> {
    init(): Promise<void>;
    /**
     * `when` callback
     *
     * @public
     * @decorator `@bind`
     */
    when(): Promise<boolean>;
}
//# sourceMappingURL=index.d.ts.map