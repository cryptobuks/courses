/// <reference types="node" />
/// <reference types="node" />
/// <reference types="express-serve-static-core" />
import type { Server } from '@roots/bud-framework/services';
import type { RequestListener } from 'http';
import { Server as HttpsServer } from 'https';
import { BaseServer } from './server.base.js';
/**
 * HTTPS Server
 * @public
 */
export declare class Https extends BaseServer implements Server.Connection {
    /**
     * Server instance
     * @public
     */
    instance: HttpsServer;
    /**
     * Https protocol
     * @public
     */
    protocol: 'https:';
    /**
     * Has options
     * @returns boolean
     * @public
     * @decorator `@bind`
     */
    hasOptions(): boolean;
    /**
     * Create HTTPS server
     * @public
     * @decorator `@bind`
     */
    createServer(express: RequestListener & Express.Application): Promise<HttpsServer>;
}
//# sourceMappingURL=server.https.d.ts.map