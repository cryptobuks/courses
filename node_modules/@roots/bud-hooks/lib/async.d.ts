import type { AsyncCallback, AsyncRegistry, AsyncStore } from '@roots/bud-framework/registry';
import type { Bud } from '@roots/bud-framework/src';
import Hooks from './base.js';
/**
 * Asynchronous hooks registry
 *
 * @public
 */
export default class Async extends Hooks<AsyncStore> {
    /**
     * Set a value
     *
     * @public
     * @decorator `@bind`
     */
    set<T extends keyof AsyncStore & string>(id: T, input: AsyncCallback[T]): Bud;
    /**
     * Set multiple values
     *
     * @public
     * @decorator `@bind`
     */
    setRecords(map: Partial<AsyncCallback>): Bud;
    /**
     * Get a value
     *
     * @public
     * @decorator `@bind`
     */
    get<T extends keyof AsyncRegistry & string>(id: T, fallback?: AsyncRegistry[T]): Promise<AsyncRegistry[T]>;
}
//# sourceMappingURL=async.d.ts.map