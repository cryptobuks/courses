import type { Bud } from '@roots/bud-framework/bud';
import type { SyncCallback, SyncRegistry, SyncStore } from '@roots/bud-framework/registry';
import Hooks from './base.js';
/**
 * Synchronous hooks registry
 *
 * @public
 */
export default class Sync extends Hooks<SyncStore> {
    /**
     * Set a value
     *
     * @public
     * @decorator `@bind`
     */
    set<T extends keyof SyncStore & string>(id: T, input: SyncRegistry[T]): Bud;
    /**
     * Set multiple values
     *
     * @public
     * @decorator `@bind`
     */
    setRecords<K extends keyof SyncRegistry & string>(map: Partial<SyncCallback>): Bud;
    /**
     * Get a value
     *
     * @public
     * @decorator `@bind`
     */
    get<T extends keyof SyncStore & string>(id: T, fallback?: SyncCallback[T]): any;
}
//# sourceMappingURL=sync.d.ts.map