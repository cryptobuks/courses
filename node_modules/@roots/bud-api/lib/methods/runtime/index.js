/**
 * Default options for runtime if no options are passed as parameters.
 */
const DEFAULT_RUNTIME = {
    name: (entrypoint) => `runtime/${entrypoint.name}`,
};
export const runtime = function (runtime = DEFAULT_RUNTIME) {
    const app = this;
    app.hooks.on(`build.optimization.runtimeChunk`, runtime === true ? DEFAULT_RUNTIME : runtime);
    return app;
};
//# sourceMappingURL=index.js.map