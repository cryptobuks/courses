export const externals = function (externals) {
    const app = this;
    app.hooks.on(`build.externals`, (existant) => ({
        ...existant,
        ...externals,
    }));
    return app;
};
//# sourceMappingURL=externals.method.js.map