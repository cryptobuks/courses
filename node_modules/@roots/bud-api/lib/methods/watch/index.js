export const watch = function (...input) {
    const app = this;
    if (!app.isDevelopment)
        return app;
    app.hooks.on(`dev.watch.files`, files => {
        if (!files)
            files = new Set();
        input.flat().forEach((file) => files.add(file));
        return files;
    });
    return app;
};
//# sourceMappingURL=index.js.map