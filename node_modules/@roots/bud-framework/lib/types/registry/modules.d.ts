import type { Extension } from '../../extension';
export declare type Modules = Record<string, Extension>;
export declare type Registry = Modules & {
    [K in keyof Modules as `${K & string}`]: Modules[K];
};
export declare type Constructors = {
    [K in keyof Registry as `${K & string}`]?: new (...args: any[]) => Registry[K];
};
//# sourceMappingURL=modules.d.ts.map