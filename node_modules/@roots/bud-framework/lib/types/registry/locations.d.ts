/**
 * Registered locations
 *
 * @virtual @public
 */
export interface Locations {
    '@src': string;
    '@dist': string;
    '@storage': string;
    '@modules': string;
}
export declare type Sync = Locations;
export declare type SyncRegistry = {
    [P in keyof Sync as `location.${P & string}`]: Sync[P];
};
//# sourceMappingURL=locations.d.ts.map