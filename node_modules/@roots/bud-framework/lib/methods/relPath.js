import { relative } from 'node:path';
export const relPath = function (...parts) {
    const app = this;
    const processedParts = parts.map(part => app.path(part));
    return relative(processedParts?.length > 1 ? processedParts.shift() : app.path(), processedParts.shift());
};
//# sourceMappingURL=relPath.js.map