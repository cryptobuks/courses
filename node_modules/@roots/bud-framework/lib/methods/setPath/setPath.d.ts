import type { Bud } from '../../bud.js';
import type * as Locations from '../../types/registry/locations';
export interface setPath {
    <T extends `${keyof Locations.Sync & string}`>(arg1: T | Partial<Record<T, string>>, arg2?: string): Bud;
}
/**
 * Set a {@link Locations} value
 *
 * @remarks
 * All values should be relative to the project directory.
 *
 * @example
 * Set project path
 * ```js
 * bud.setPath('/app/absolute/path/')
 * ```
 *
 * @example
 * ```js
 * bud.setPath('@src', 'custom/src')
 * ```
 *
 * @param arg1 - path handler
 * @param arg2 - path value
 *
 * @public
 */
export declare const setPath: setPath;
//# sourceMappingURL=setPath.d.ts.map