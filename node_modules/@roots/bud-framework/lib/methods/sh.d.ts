import { ExecaChildProcess, Options } from '@roots/bud-support/execa';
export interface sh {
    (command: string | Array<string>, options?: Options): ExecaChildProcess;
}
/**
 * Execute a shell command
 *
 * @example
 * ```js
 * bud.sh(`ls -la`)
 * ```
 *
 * @public
 */
export declare const sh: sh;
//# sourceMappingURL=sh.d.ts.map